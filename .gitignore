# Eclipse files
bin/
.classpath
.project
.settings

# gradle's output dir
build/

# the gradle temp dir
.gradle

# Avoid ignoring Gradle wrapper jar file (.jar files are usually ignored)
!gradle-wrapper.jar
